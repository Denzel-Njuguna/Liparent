JWT_SECRET=apex_development 
# consider using 

# Generate RSA keys (run in terminal):
# openssl genrsa -out private.pem 4096
# openssl rsa -in private.pem -pubout -out public.pem

# config/dev.exs
# config :joken, default_signer: [
#   signer: [
#     alg: "RS256",
#     key: %{
#       "pem" => File.read!("./private.pem")
#     }
#   ]
# ]

# # config/prod.exs
# config :joken, default_signer: [
#   signer: [
#     alg: "RS256",
#     key: %{
#       "pem" => System.get_env("JWT_PRIVATE_KEY")
#     }
#   ]
# ]